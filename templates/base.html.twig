<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}UEL222{% endblock %}</title>

    {# Bootstrap via le CDN du site officiel #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}
        <style> {# Test personnalisation Bootstrap#}
        .navbar-custom {
            background-color: #E85412;
        }
        </style>
    {% endblock %}
</head>
<body>

{# Menu dynamique TWIG : #}
{% set menu = [
    {'label': 'Tous les articles', 'route': 'article_index'},
    {'label': 'Ajouter un article', 'route': 'article_new'},
    {'label': 'Toutes les catégories', 'route': 'category_index'},
    {'label': 'Ajouter une catégorie', 'route': 'category_new'},
] %}

<nav class="navbar navbar-expand-lg navbar-dark navbar-custom mb-4"> {#Navbar expand lg permet d'accéder aux liens s'il y a de la place sur l'écran
sinon cela forme un menu dépliant, navbar-dark pour une écriture clair et mb-4 pour le padding #}
    <div class="container">
        <a class="navbar-brand" href="{{ path('article_index') }}">UEL222</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</nav>

<div class="container"> {# Permet d'aligner les pages #}
    {% block body %}{% endblock %}
</div>

{# Bootstrap JS #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
